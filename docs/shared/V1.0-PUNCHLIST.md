# V1.0 Punchlist — consolidated remainder (line drawn S279)

**Purpose.** Single burn-down list for what stands between current main and a V1.0 release
candidate. Consolidates three previously-uncoordinated piles: the **card-walk remainder** (tracked
only in WALK-FINDINGS + the S271 closeout audit), the **BANKED design-call queue**, and the
**CI-band replacement**. Until this list existed, "what's left for V1.0?" required reconstructing
across four docs — this makes it a lookup.

**Status of inputs at reconciliation:**
- Walk remainder: the S271 closeout audit (`SESSION271-WALK-CLOSEOUT-AUDIT.md`, classified at
  `49cb1c5`) gave three genuine-open passes. **Reconciled forward to S278:** nothing in S272–S278
  touched the walk's GENUINE-OPEN set — those sessions ran the #25 redesign cluster, the
  reference-line/legend-vocab arc, and the RSC single-verdict arc, all of which were the
  ROUTES-ELSEWHERE work the audit had already separated out. **The three-pass count is current.**
- BANKED design-calls: read at S278; the table-standardisation cluster and CrossCond split are live.
- CI-band: still the one open correctness item (STATUS Known bugs).

---

## The line (drawn S279)

**The V1.0 bar:** *no surface contradicts its verdict, and the copy a reviewer reads first is clean.*
Polish that only makes a working surface nicer is v1.x. Every CALL item was run against that bar.

The bar pulls two items into V1.0 that read as "polish" at first glance but are verdict-faithfulness
issues on a forensics tool: **C1/C2** (a Finding word that reads as benign beside a significant p
makes a reader doubt the verdict) and **#31** (a wrong column index on a forensics table is a
correctness smell, not cosmetics). It also splits B2 — the EvidenceTable add is IN (it's a live VFS
clip fix + shared infra), the CrossCond/Duplicated restructures it would enable are DEFER.

**IN — V1.0 release candidate:**
- A1 — CI-band replacement (the one correctness blocker; critical path, heaviest)
- B1 — verdict-phrasing copy pass
- C1 — table-content design-call triage (Finding-vocabulary verdict-faithfulness)
- C2 — Finding-column-position convention (resolves in the C1 sitting)
- D — single-verdict consolidation remainder
- #31 — Col-N file-column label
- 13b — "Noise distribution" rename (pre-paper)
- EvidenceTable add (live VFS DS04 Adj.p clip fix + shared infra)

**DEFER — v1.x / v1.0.x polish train:**
- B2 — CrossCond two-table split + Duplicated Data residue (#32/#33)
- B3 — ClusterRow icon-gap, SelectiveNoise plot-rationale
- E — colour/legend arcs (data-model, two-tone, Arc A axis, Arc C legend)

**Estimate at this line:** ~6 sessions to a release candidate (A1 dominates; B1 + the C1/C2 sitting +
D + the small IN items are ~1 session each, EvidenceTable folds into the CrossCond-adjacent work or
stands alone). The DEFER set is the v1.0.x train, ~3–4 further sessions if pulled forward.

**The one contestable call:** C1/C2 IN. If the Finding-vocabulary variance is read as genuinely
cosmetic (a reviewer won't misread a benign label beside a significant p), C1/C2 drops to DEFER and
the estimate returns to ~4–5. That single judgement is what moves the number. Drawn IN on the
forensics-tool argument that verdict-adjacent label ambiguity is not polish.

**Flagged for a look before E fully defers:** the data-model arc overturns the Test25b NOT-A-DEFECT
ruling. Absent a live contradiction it's v1.x, but confirm there's no live verdict conflict on that
surface before the DEFER tag is load-bearing.

---

## A. Correctness — the one substantive blocker

### A1. CI-band replacement (Autocorrelation + Runs) — **IN**
The two shipped CI bands are **live defects**: both plot a pooled quantity while the flag promotes on
per-unit evidence the band doesn't show, so the band can read clean while the card flags. The CI-band
*programme* was retired S237 and superseded by the **per-unit display programme** — but the
replacement hasn't been built, so the two defective bands are still live on main.

This is the only open item that makes a card's *display* contradict its *verdict* on real fixtures.
Everything else on this list is copy or legibility. **Biggest single piece of work remaining**
(per-unit display programme, not a one-session fix). Source-of-truth: BANKED CI section +
TIER-A-CI-DRAW-SPEC.md §6/§7. **Wants its own structure-first scoping read** (which cards, what the
per-unit display looks like per test class) before it is a build — do not start it as a quick win.

---

## B. Card-walk remainder — three passes (~2–3 sessions, per S271, current at S278)

### B1. Verdict-phrasing card-copy pass — **IN** (~1 session)
~10 verdict/headline string edits. The verdict strings still differ from the walk's requested
question-phrasing on every test sampled (confirmed live at S271). One copy-convention pass, the direct
analogue of the method-prose pass that shipped S270. **This pass is currently unhomed in STATUS** —
it exists only as blank-routed WALK-FINDINGS rows + the S271 audit. Highest-leverage visible win;
reviewer-facing (these are the verdict lines a reader reads first). Chat-authored copy.
The rows (live verdict → walk-requested form):
- First/Second-Digit Frequencies — "…depart from the expected pattern" → "…not distributed as expected?"
- Last-Digit Frequencies — "Last digits are not evenly spread" → "…not distributed as expected?"
- Decimal precision — "Consistent precision throughout" → "Consistent decimal precision throughout dataset?"
- Over-used numbers — "…more often than chance allows" → "…more often than expected?"
- Shared noisy rows — what-to-look-for mismatch (RSC lookFor)
- small siblings: 3a lookFor, 10a-DS20 skipped-col copy, 20a region-count reconcile
- **Open copy decision:** whether to adopt the "…as expected?" question form at all (it reads as a
  question on a verdict line — may want declarative). One session regardless of how the wording lands.

### B2. Two dense-card legibility restructures — **DEFER** (restructures) / **IN** (EvidenceTable add)
The only heavy display work left in the walk. Split at the S279 line:
- **EvidenceTable fixed-layout add — IN.** Not polish: it clears the live VFS DS04 Adj.p clip (a real
  display defect) and is shared infrastructure the CrossCond split depends on. Build prompt
  pre-emitted at `S275-EVIDENCETABLE-BUILD-PROMPT`, apparently not yet run — verify before dispatch.
- **CrossCond two-table split** (`MiniCard_CrossCondConsistency.jsx`) — **DEFER.** Flagged rows
  un-capped on top, tail scrolls beneath. A legibility upgrade on a card that already reads correctly;
  v1.0.x. Unblocks once the EvidenceTable add lands.
- **Duplicated Data residue** (`MiniCard_DuplicateDetection.jsx`) — **DEFER.** Font-hierarchy /
  over-red-table / inaccurate-24-row-description, partly addressed by the S274 #25 redesign; residue is
  #32 dead-code tidy + #33 LOW-card prominence (both BANKED). Polish on a working card; v1.0.x.

### B3. Small display defects + carry-ins — split at the line
- **#31 Col-N file-column label — IN.** Raw matrix col index shown instead of file column; a wrong
  column index on a forensics table is a correctness smell, not cosmetics. The label *choice* (header
  name / file column / Excel letter) is an unmade Chat decision that generalises across
  CarlisleBalance + ColumnGoF + Entropy — **Chat decides first, then one dispatch.**
- **13b "Noise distribution" rename — IN.** `DISPLAY_NAMES` + `METHOD_BATTERY`; small, pre-paper. The
  name pulls the eye to "Distribution shapes" by adjacency and is absent from the §5 battery list.
- **ClusterRow icon-gap-on-wrap** (`ClusterRow.jsx`) — **DEFER.** §3 cluster-header, ~20 lines;
  cosmetic on a working card.
- **SelectiveNoise bar-vs-whisker plot rationale** — **DEFER.** Decide/document HBarPlot vs
  distribution plots; v1.x documentation item.
- ColumnGoF "Direction" column placement → folded into **C2** (table-standardisation sibling-pair,
  not a one-card reorder).

---

## C. BANKED design-call triage — one sitting resolves several — **IN**

Drawn IN on the forensics-tool argument: a `Finding` label that reads as benign beside a significant
`p` makes a reader doubt the verdict. That is a verdict-faithfulness issue, not a consistency nicety.
One design sitting resolves C1 and C2 together.

### C1. Table-content design-call triage — **IN** (one design sitting, then small edits)
A cluster of BANKED items that all wait on **one** "table content" design decision, then resolve
together (mostly small edits):
- **Finding-vs-significance vocabulary** — the `Finding` column value words vary card-to-card
  ("As expected"/"Normal"/"similar"); lock `Finding` = effect-size descriptor, unify the vocabulary,
  document the intent so a benign label beside a significant p doesn't read as a bug.
- **Raw-vs-adjusted-p display** (Runs Table 1, Mahalanobis) — the displayed `p` column and the `adjP`
  that drives the Finding word differ; decide whether to show `Adj. p`.
- **8a "Pass" column** — load-bearing (detection-method, not a verdict tier); document so it doesn't
  read as a missing verdict column.

### C2. Finding-column-position convention — **IN** (battery-wide, then per-card edits)
ColumnGoF + Entropy put `Finding` at position 2 (deliberate mirror); Kurtosis, WithinRowVariance,
CrossCond put it rightmost. The suite is **split on the convention** — this is a battery-wide decision,
not a one-card reorder, and each card's column array is inline (no shared builder), so standardising
means editing each array. Homes the walk's 10a Direction-column row. Resolve with C1 (same sitting).

---

## D. Single-verdict consolidation remainder — **IN** (bounded, spec rule landed)
The S278 arc shipped the RSC item; the spec rule (parallel-verdict) is on main. Remaining, all governed
by that rule:
- **MADConformity** — the one other confirmed live cleared-card exposure, on the Copy-summary clipboard
  text export (not the card UI); label-as-diagnostic. Own read first (ReportView generateTextSummary).
- **rankCorrelation §4 `interpretation` word** — flagged-only annotation; low urgency.
- **milder Group-1 set** (kurtosis per-condition, selectiveNoise direction, terminalDigits, loess,
  regionalNoise, withinRowVariance) — flagged-only; lowest urgency.
- `constantOffset.severityClass` retire — at next constantOffset touch, not a standalone item.

---

## E. Routed-elsewhere colour/legend arcs — **DEFER** (overlaps the walk's ROUTES-ELSEWHERE set)
Tracked outside the walk but still open, scoped S245. Colour-consistency polish on working surfaces —
v1.x, with one caveat:
- **Data-model arc** — observed marks blue-clear/red-flagged across 8 surfaces; spec landed, retoken is
  the build. Two-state screenshot gate. **Overturns Test25b NOT-A-DEFECT — confirm no live verdict
  conflict on that surface before the DEFER tag is load-bearing.**
- **Two-tone observed encodings** — SignStrip + RowMeanTrend → `CC.OBS` + Oxford navy.
- **Arc A axis eyes-on tail**, **Arc C legend location** — need live render.

---

## Ordered burn-down (post-line)

1. **B1 verdict-phrasing copy pass** — highest-leverage single win, 1 session, Chat-authored, currently
   unhomed (easiest to lose). Start with the open copy decision (question-form vs declarative).
2. **D — MADConformity first** — bounded; own read of the Copy-summary clipboard path before the fix.
3. **#31 + 13b** — Chat decision (#31 label choice) then one combined small dispatch; both pre-paper.
4. **C1/C2 design sitting** — one Chat design decision, then per-card array edits.
5. **EvidenceTable add** — verify `S275-EVIDENCETABLE-BUILD-PROMPT` not yet run, then dispatch.
6. **A1 CI-band replacement** — critical-path correctness, heaviest; its own structure-first scoping
   read precedes any build. Last because it's the longest pole, not the lowest priority.

The DEFER set (B2 restructures, B3 cosmetics, E colour arcs) becomes the v1.0.x polish train once the
release candidate ships.

---

## What this list deliberately excludes (→ v1.x, per V1X-FUTURE-WORK)
Methodology gaps (§1), rectangular Blocked Mahalanobis (§2.1), AI Screening mode (§4), calibration
audits (B=9999, severity-diversity, Modality plot upgrade), assay-aware severity weighting, LOESS
recursive segmentation, Terminal-Digit directional statistic, genomics normalization advisory. All
owned by V1X-FUTURE-WORK.md; none is v1.0.
