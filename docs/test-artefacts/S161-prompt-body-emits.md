# S161 §4 prompt-body emits — 22-fixture trimmed dump

Each entry trimmed to the variant sections only (Dataset / Outcome calibration / Findings / Cleared / Other clusters / Tests not run). Invariant template surfaces (role-setting intro, About Check My Data, How to think, Attachments, Common directions, Discipline) live verbatim in [src/analysis/promptBodyRenderer.js](../../src/analysis/promptBodyRenderer.js). Generated by [test/diag-s161-prompt-body-trimmed.mjs](../../test/diag-s161-prompt-body-trimmed.mjs); rerun to refresh.

## DS01 — 01-densitometry-clean.csv (outcome 0)

§4 does not render at outcome 0; renderer returns null.

---

## DS02 — 02-densitometry-fabricated.csv (outcome 3)

### Dataset

- Filename: 02-densitometry-fabricated.csv
- Shape: 35 rows × 12 columns
- Measurement type: Western Blot Densitometry (user-set)
- Data type: Continuous
- Conditions: Control, Inhibitor_A, Inhibitor_B (3 conditions)
- Variance-stabilising transform: log (slope=0.54, CI [0.28, 0.81] below 1 → inconclusive → assay fallback (densitometry) → log)

### Outcome calibration

Outcome 3 of 4 — Investigate closely. Significant anomalies detected. The combination of findings is unlikely to arise from chance alone. Treat as a strong screening signal warranting close investigation. Innocent explanations remain possible — the burden is on identifying a specific one that fits the patterns.

### Findings

#### High-severity findings

None.

#### Moderate-severity findings

**Inter-Replicate Correlation** — cross-replicate cluster
What the test detects: Checks whether replicate columns track each other more closely than expected. Compares each pair's correlation against the leave-one-out average of all other pairs in the same condition (winsorised Pearson r).
Location: Global
Evidence: Windowed permutation scan detected locally elevated inter-replicate correlation (scan p=0.0070): Inhibitor_A 1–2 rows 16–26 (r=0.9828); Control 2–3 rows 7–17 (r=0.9333); Control 2–3 rows 10–20 (r=0.9170). This suggests non-independence in a subset of rows that is diluted by the global analysis.
primaryP = 0.0070

**Residual Spike Correlation** — copy-paste/edit cluster
What the test detects: Identifies which rows are unusually variable between replicate pairs and tests whether the same rows are noisy across multiple conditions. In typical data, noisy rows vary independently across conditions — coordinated residual spikes suggest shared structure. Significance assessed by shuffling row order (permutation test).
Location: Global
Evidence: primaryP = 0.0010

**Autocorrelation** — cross-replicate cluster
What the test detects: Checks whether the inter-replicate noise is correlated between adjacent rows. In independent measurements, knowing the noise at one row tells you nothing about the next — significant correlation means the noise follows a pattern rather than varying freely (lag-1 autocorrelation, pooled t-test).
Location: 3 regions
Evidence: primaryP = 0.0179

**Runs Test** — cross-replicate cluster
What the test detects: For each replicate pair, tracks which replicate has the larger value at each row and checks how often the lead switches. Too few switches means one replicate stays consistently above the other — either across the full column or within localised stretches (Wald–Wolfowitz runs test).
Location: 3 regions
Evidence: primaryP = 0.0059

#### Cleared (within flagged clusters)

Within cross-replicate cluster:
- Mahalanobis Row Outlier
- Excess Kurtosis
- Windowed Autocorrelation
- LOESS Residual Analysis
- Selective Noise Partitioning
- Regional Noise Homogeneity

Within copy-paste/edit cluster:
- Exact Duplicate Detection
- Constant-Offset Blocks

Cleared tests are evidence too — a test that was run and didn't fire narrows the space of explanations.

#### Other clusters — all applicable tests cleared

- Unusual digits cluster (2 tests): Terminal Digit Uniformity, Decimal Precision Consistency
- Distribution shapes cluster (2 tests): Entropy / Zipf Analysis, Column Goodness-of-Fit
- Cross-condition cluster (1 tests): Cross-Condition Consistency

#### Tests not run

- Benford's Law (First Digit) — OOM span 0.7 < 1.5 — Benford's Law requires ≥1.5 orders of magnitude for meaningful application.
- Benford's Law (Second Digit) — Data spans only 0.7 orders of magnitude — Benford's Law requires ≥1 for meaningful application.
- Value-Frequency Spike — Not applicable — data is primarily non-integer. This test detects anomalous frequency spikes in integer value distributions.
- Baseline Balance — 35/35 features (100%) show significant condition differences — conditions are genuinely different. Balance test not applicable.
- Cross-Condition Rank Correlation — With fewer than 4 condition pairs, LOO null has too few degrees of freedom to distinguish fabrication from routine biological similarity (most features non-differentially expressed). This test requires ≥4 condition pairs to make statistical inference. Shown here as contextual information only.
- Blocked Mahalanobis — Need ≥60 complete rows per condition (have 35 total rows across conditions).
- Noise Scaling With Measurement Size — Row means span only 0.4 orders of magnitude — need ≥1.0 for a reliable log-log slope estimate. Noise Scaling test is inapplicable for narrow-range data.
- Modality Test — All columns routed to N/A (12 columns; most common: < 50 observations).
- Within-Row Variance — Insufficient rows (35). Need ≥40 for stable mean-variance fit.
- Row-Mean Runs — Need ≥2 replicate columns and ≥10 rows.
- Missing Data Pattern — Missing rate 0.0% is below 1% threshold.

Absence of a finding for a test that wasn't run does not mean absence of the pattern.

---

## DS03 — 03-qpcr-clean.csv (outcome 0)

§4 does not render at outcome 0; renderer returns null.

---

## DS04 — 04-qpcr-fabricated.csv (outcome 3)

### Dataset

- Filename: 04-qpcr-fabricated.csv
- Shape: 50 rows × 3 columns
- Measurement type: qPCR (Ct values) (user-set)
- Data type: Continuous
- Conditions: WT, KO (2 conditions)
- Variance-stabilising transform: raw (slope=-1.12, CI [-2.44, 0.21] below 1 → inconclusive → assay fallback (qpcr) → raw)

### Outcome calibration

Outcome 3 of 4 — Investigate closely. Significant anomalies detected. The combination of findings is unlikely to arise from chance alone. Treat as a strong screening signal warranting close investigation. Innocent explanations remain possible — the burden is on identifying a specific one that fits the patterns.

### Findings

#### High-severity findings

**Terminal Digit Uniformity** — unusual digits cluster
What the test detects: Counts the frequency of each last digit (0–9) across all values and tests for deviation from a uniform distribution (chi-squared test).
Location: Global
Evidence: primaryP = <0.0001

**Exact Duplicate Detection** — copy-paste/edit cluster
What the test detects: Scans the dataset for identical values and tests the observed count against what would be expected if values were shuffled randomly (permutation test). Tests are computed independently; some duplicates may appear in more than one test.
Location: 3 duplicate groups
Evidence: primaryP = <0.0001

#### Moderate-severity findings

**Value-Frequency Spike** — unusual digits cluster
What the test detects: Tests whether any value in a column appears far more often than its neighbours. Hand-typed fabricated data tends to reuse "comfortable" values — round numbers, favourite digits — producing sharp frequency spikes against the smooth distribution real measurements produce. The test smooths each value’s frequency by comparing against its neighbours in value-space, then flags any value whose observed count is too high to be explained by the smoothed expectation. See METHODOLOGY.md §3.5 for the formal statement (leave-one-out neighbour smoothing; Poisson tail probability for the spike-vs-expected comparison).
Location: Global
Evidence: 3 values with anomalous frequency: .96 (8× obs, 1.0 exp, 8.0×, digit pass), .91 (7× obs, 1.0 exp, 7.0×, digit pass), .56 (6× obs, 0.8 exp, 7.2×, digit pass). Fractional digit substring repeats across differing integer parts — consistent with template reuse (copy-paste of a decimal-digit pattern) rather than independent measurement.
primaryP = 0.0010

#### Cleared (within flagged clusters)

Within unusual digits cluster:
- Decimal Precision Consistency

Within copy-paste/edit cluster:
- Constant-Offset Blocks
- Residual Spike Correlation

Cleared tests are evidence too — a test that was run and didn't fire narrows the space of explanations.

#### Other clusters — all applicable tests cleared

- Distribution shapes cluster (3 tests): Entropy / Zipf Analysis, Column Goodness-of-Fit, Modality Test
- Cross-replicate cluster (12 tests): Inter-Replicate Correlation, Mahalanobis Row Outlier, Noise Scaling With Measurement Size, Excess Kurtosis, Autocorrelation, Windowed Autocorrelation, Runs Test, Within-Row Variance, LOESS Residual Analysis, Row-Mean Runs, Selective Noise Partitioning, Regional Noise Homogeneity
- Cross-condition cluster (1 tests): Cross-Condition Consistency

#### Tests not run

- Benford's Law (First Digit) — OOM span 0.4 < 1.5 — Benford's Law requires ≥1.5 orders of magnitude for meaningful application.
- Benford's Law (Second Digit) — Data spans only 0.4 orders of magnitude — Benford's Law requires ≥1 for meaningful application.
- Baseline Balance — Only 3 DATA columns — need ≥5 features for meaningful balance test.
- Cross-Condition Rank Correlation — With fewer than 4 condition pairs, LOO null has too few degrees of freedom to distinguish fabrication from routine biological similarity (most features non-differentially expressed). This test requires ≥4 condition pairs to make statistical inference. Shown here as contextual information only.
- Blocked Mahalanobis — Need ≥60 complete rows per condition (have 50 total rows across conditions).
- Missing Data Pattern — Missing rate 0.0% is below 1% threshold.

Absence of a finding for a test that wasn't run does not mean absence of the pattern.

---

## DS05 — 05-cellcount-clean.csv (outcome 0)

§4 does not render at outcome 0; renderer returns null.

---

## DS06 — 06-cellcount-fabricated.csv (outcome 3)

### Dataset

- Filename: 06-cellcount-fabricated.csv
- Shape: 55 rows × 4 columns
- Measurement type: Cell Counting / Viability (user-set)
- Data type: Count / Integer
- Conditions: single condition (no condition column)
- Variance-stabilising transform: Anscombe √(x+⅜) (Integer data (100%), slope=0.12, 95% CI [-0.28, 0.52] → Anscombe √(x+⅜))

### Outcome calibration

Outcome 3 of 4 — Investigate closely. Significant anomalies detected. The combination of findings is unlikely to arise from chance alone. Treat as a strong screening signal warranting close investigation. Innocent explanations remain possible — the burden is on identifying a specific one that fits the patterns.

### Findings

#### High-severity findings

**Exact Duplicate Detection** — copy-paste/edit cluster
What the test detects: Scans the dataset for identical values and tests the observed count against what would be expected if values were shuffled randomly (permutation test). Tests are computed independently; some duplicates may appear in more than one test.
Location: 1 block
Evidence: primaryP = <0.0001

**Mahalanobis Row Outlier** — cross-replicate cluster
What the test detects: Checks whether any rows have an unusual combination of values across replicate columns. Each individual value might look plausible, but together they can fall outside the pattern that other rows follow — for example, replicates that all sit on the same side of their respective means (Mahalanobis distance against χ² distribution).
Location: 1 row
Evidence: primaryP = 0.0004

**Noise Scaling With Measurement Size** — cross-replicate cluster
What the test detects: Checks whether noise scales with signal magnitude in the way the instrument type predicts — for example, constant variability for qPCR or proportional variability for ELISA. Fits a trend line on a log-log scale and compares the observed slope to the expected slope for the selected assay (z-test with block-robust standard error).
Location: Global
Evidence: Expected log-log slope ≈ 1 for cell_count (0=additive, 1=Poisson, 2=proportional). Observed: 0.12 ± 0.204 SE. Deviation: 0.88, z=-4.33, p=0.0000.
primaryP = <0.0001

#### Moderate-severity findings

**Value-Frequency Spike** — unusual digits cluster
What the test detects: Tests whether any value in a column appears far more often than its neighbours. Hand-typed fabricated data tends to reuse "comfortable" values — round numbers, favourite digits — producing sharp frequency spikes against the smooth distribution real measurements produce. The test smooths each value’s frequency by comparing against its neighbours in value-space, then flags any value whose observed count is too high to be explained by the smoothed expectation. See METHODOLOGY.md §3.5 for the formal statement (leave-one-out neighbour smoothing; Poisson tail probability for the spike-vs-expected comparison).
Location: Global
Evidence: 1 value with anomalous frequency: 590 (3× obs, 0.1 exp, 30.0×).
primaryP = 0.0095

#### Cleared (within flagged clusters)

Within copy-paste/edit cluster:
- Constant-Offset Blocks

Within cross-replicate cluster:
- Inter-Replicate Correlation
- Excess Kurtosis
- Autocorrelation
- Windowed Autocorrelation
- Runs Test
- Within-Row Variance
- LOESS Residual Analysis
- Selective Noise Partitioning
- Regional Noise Homogeneity

Within unusual digits cluster:
- (no other applicable tests in this cluster ran for this dataset — see "Tests not run" below)

Cleared tests are evidence too — a test that was run and didn't fire narrows the space of explanations.

#### Other clusters — all applicable tests cleared

- Distribution shapes cluster (3 tests): Entropy / Zipf Analysis, Column Goodness-of-Fit, Modality Test

#### Tests not run

- Benford's Law (First Digit) — Not applicable to cell count data. Poisson count data from a single counting process has a mathematically determined leading-digit distribution that depends on λ, not Benford's law. Benford applicability requires data spanning multiple orders of magnitude from heterogeneous natural processes.
- Benford's Law (Second Digit) — Not applicable to cell count data. See Benford's First Digit for rationale.
- Terminal Digit Uniformity — Data is 100% integer values. Terminal digit analysis tests the last decimal digit of continuous measurements — for integer/count data, the units digit distribution depends on the count-generating process and is not expected to be uniform. Test not applicable.
- Decimal Precision Consistency — Insufficient decimal-valued data (need ≥30 non-integer values).
- Residual Spike Correlation — No condition grouping found. Residual spike correlation requires ≥2 conditions.
- Baseline Balance — Requires ≥2 experimental conditions.
- Cross-Condition Rank Correlation — Need ≥2 conditions with ≥5 rows each.
- Cross-Condition Consistency — Need ≥2 experimental conditions.
- Blocked Mahalanobis — Not applicable to count data (covariance structure null is only stable on continuous measurements).
- Row-Mean Runs — Requires row-level condition labels. Without per-condition grouping, biological variation in row means produces natural clustering that cannot be distinguished from data concerns.
- Missing Data Pattern — Missing rate 0.0% is below 1% threshold.

Absence of a finding for a test that wasn't run does not mean absence of the pattern. In particular, this dataset has a single condition, so the cross-condition cluster (3 tests) and condition-dependent tests in other clusters did not run.

---

## DS07 — 07-elisa-clean.csv (outcome 0)

§4 does not render at outcome 0; renderer returns null.

---

## DS08 — 08-elisa-fabricated.csv (outcome 3)

### Dataset

- Filename: 08-elisa-fabricated.csv
- Shape: 65 rows × 3 columns
- Measurement type: ELISA / Immunoassay (user-set)
- Data type: Continuous
- Conditions: single condition (no condition column)
- Variance-stabilising transform: log (slope=2.10, CI [1.95, 2.24] contains 1 → inconclusive → assay fallback (elisa) → log)

### Outcome calibration

Outcome 3 of 4 — Investigate closely. Significant anomalies detected. The combination of findings is unlikely to arise from chance alone. Treat as a strong screening signal warranting close investigation. Innocent explanations remain possible — the burden is on identifying a specific one that fits the patterns.

### Findings

#### High-severity findings

**Benford's Law (First Digit)** — unusual digits cluster
What the test detects: Counts the frequency of each first digit (1–9) across all values. In naturally occurring datasets, lower leading digits (1, 2, 3…) appear more often than higher ones (Benford distribution). This test checks whether the data follows that pattern (chi-squared goodness-of-fit with MAD simulation).
Location: Global
Evidence: primaryP = <0.0001

**Inter-Replicate Correlation** — cross-replicate cluster
What the test detects: Checks whether replicate columns track each other more closely than expected. Compares each pair's correlation against the leave-one-out average of all other pairs in the same condition (winsorised Pearson r).
Location: Global
Evidence: 1 within-condition replicate pair have r significantly above the leave-one-out predicted value (BH-adjusted p<0.001), suggesting those replicates may not be genuinely independent.
primaryP = 0.0006

**LOESS Residual Analysis** — cross-replicate cluster
What the test detects: Measures how variable each row's replicates are, then fits a smooth trend to see whether noise changes gradually across the dataset. A cumulative-sum test (CUSUM) pinpoints the exact row where the noise character shifts — the changepoint.
Location: 5 regions
Evidence: Windowed scan detects a region with smoother noise (rows 37–56, 7.2× variance ratio, p=0.0020). CUSUM changepoint: noise decreases at row 32 (p=0.0002). A subset of rows has different noise character than the rest, suggesting smoothing, extrapolation, or manual editing in that region.
primaryP = 0.0002

**Selective Noise Partitioning** — cross-replicate cluster
What the test detects: Checks whether all replicate columns have similar variability. A significant result means the noise levels are unequal (Bartlett's test), but with few columns it may not be possible to pinpoint which one differs.
Location: Global
Evidence: primaryP = 0.0006

#### Moderate-severity findings

**Constant-Offset Blocks** — copy-paste/edit cluster
What the test detects: Checks for clusters of constant offsets. For each row, computes the difference between replicate pairs and checks whether that difference repeats in neighbouring rows. Significance assessed by shuffling row order (permutation test).
Location: Global
Evidence: primaryP = 0.0010

**Mahalanobis Row Outlier** — cross-replicate cluster
What the test detects: Checks whether any rows have an unusual combination of values across replicate columns. Each individual value might look plausible, but together they can fall outside the pattern that other rows follow — for example, replicates that all sit on the same side of their respective means (Mahalanobis distance against χ² distribution).
Location: 1 row
Evidence: primaryP = 0.0017

#### Cleared (within flagged clusters)

Within unusual digits cluster:
- Benford's Law (Second Digit)
- Terminal Digit Uniformity
- Decimal Precision Consistency
- Value-Frequency Spike

Within cross-replicate cluster:
- Blocked Mahalanobis
- Noise Scaling With Measurement Size
- Excess Kurtosis
- Autocorrelation
- Windowed Autocorrelation
- Runs Test
- Within-Row Variance
- Regional Noise Homogeneity

Within copy-paste/edit cluster:
- Exact Duplicate Detection

Cleared tests are evidence too — a test that was run and didn't fire narrows the space of explanations.

#### Other clusters — all applicable tests cleared

- Distribution shapes cluster (2 tests): Entropy / Zipf Analysis, Modality Test

#### Tests not run

- Residual Spike Correlation — No condition grouping found. Residual spike correlation requires ≥2 conditions.
- Baseline Balance — Requires ≥2 experimental conditions.
- Cross-Condition Rank Correlation — Need ≥2 conditions with ≥5 rows each.
- Cross-Condition Consistency — Need ≥2 experimental conditions.
- Column Goodness-of-Fit — All columns routed to N/A (3 columns; most common: Pre-skip: γ₁=3.00, γ₂=10.54 — family set {Normal, Poisson, NB} does not cover this shape (v1.1 extension planned)).
- Row-Mean Runs — Requires row-level condition labels. Without per-condition grouping, biological variation in row means produces natural clustering that cannot be distinguished from data concerns.
- Missing Data Pattern — Missing rate 0.0% is below 1% threshold.

Absence of a finding for a test that wasn't run does not mean absence of the pattern. In particular, this dataset has a single condition, so the cross-condition cluster (3 tests) and condition-dependent tests in other clusters did not run.

---

## DS09 — 09-proteomics-clean.csv (outcome 0)

§4 does not render at outcome 0; renderer returns null.

---

## DS10 — 10-proteomics-fabricated.csv (outcome 3)

### Dataset

- Filename: 10-proteomics-fabricated.csv
- Shape: 400 rows × 6 columns
- Measurement type: Proteomics (Intensity / LFQ) (user-set)
- Data type: Continuous
- Conditions: Vehicle, Treatment (2 conditions)
- Variance-stabilising transform: log (slope=1.97, CI [1.92, 2.02] contains 1 → inconclusive → assay fallback (proteomics) → log)

### Outcome calibration

Outcome 3 of 4 — Investigate closely. Significant anomalies detected. The combination of findings is unlikely to arise from chance alone. Treat as a strong screening signal warranting close investigation. Innocent explanations remain possible — the burden is on identifying a specific one that fits the patterns.

### Findings

#### High-severity findings

**Benford's Law (Second Digit)** — unusual digits cluster
What the test detects: Counts the frequency of each second digit (0–9) across all values. As with first digits, lower second digits (0, 1, 2…) appear slightly more often than higher ones, following a predictable distribution (Benford distribution). This test checks whether the data follows that pattern (chi-squared goodness-of-fit with MAD simulation).
Location: Global
Evidence: primaryP = <0.0001

**Exact Duplicate Detection** — copy-paste/edit cluster
What the test detects: Scans the dataset for identical values and tests the observed count against what would be expected if values were shuffled randomly (permutation test). Tests are computed independently; some duplicates may appear in more than one test.
Location: 10 duplicate groups
Evidence: primaryP = <0.0001

#### Moderate-severity findings

**Column Goodness-of-Fit** — distribution shapes cluster
What the test detects: For each column, fits a parametric family (Normal, Poisson, or Negative Binomial) and measures how closely the column's CDF shape matches the fit (Anderson–Darling). A parametric bootstrap with refit calibrates the expected AD²; too-large values indicate shape mismatch (hand-typed, truncated, copied from a different-shape source), too-small values indicate a too-tight fit suggesting RNG padding.
Location: Global
Evidence: primaryP = 0.0020

**LOESS Residual Analysis** — cross-replicate cluster
What the test detects: Measures how variable each row's replicates are, then fits a smooth trend to see whether noise changes gradually across the dataset. A cumulative-sum test (CUSUM) pinpoints the exact row where the noise character shifts — the changepoint.
Location: 9 regions
Evidence: Windowed scan detects a region with smoother noise (rows 205–224, 6.3× variance ratio, p=0.0040). CUSUM changepoint: noise decreases at row 159 (p=0.0020); secondary at row 242 (noise increases, segment p=0.0020). A subset of rows has different noise character than the rest, suggesting smoothing, extrapolation, or manual editing in that region.
primaryP = 0.0020

**Regional Noise Homogeneity** — cross-replicate cluster
What the test detects: Divides the replicate columns into spatial blocks and checks whether some blocks are more or less variable than others. Significance assessed by shuffling row order (Levene’s test with permutation null).
Location: 2 regions
Evidence: Column-vs-global windowed scan detects locally reduced noise in column 6 (scan p=0.0020). Most anomalous window: rows 201–215 (15.3× deviation from column's global variance).
primaryP = 0.0020

#### Cleared (within flagged clusters)

Within unusual digits cluster:
- Benford's Law (First Digit)
- Terminal Digit Uniformity
- Decimal Precision Consistency
- Value-Frequency Spike

Within copy-paste/edit cluster:
- Constant-Offset Blocks
- Residual Spike Correlation

Within distribution shapes cluster:
- Entropy / Zipf Analysis
- Modality Test

Within cross-replicate cluster:
- Inter-Replicate Correlation
- Mahalanobis Row Outlier
- Blocked Mahalanobis
- Noise Scaling With Measurement Size
- Excess Kurtosis
- Autocorrelation
- Windowed Autocorrelation
- Runs Test
- Within-Row Variance
- Row-Mean Runs
- Selective Noise Partitioning

Cleared tests are evidence too — a test that was run and didn't fire narrows the space of explanations.

#### Other clusters — all applicable tests cleared

- Cross-condition cluster (2 tests): Baseline Balance, Cross-Condition Consistency

#### Tests not run

- Cross-Condition Rank Correlation — With fewer than 4 condition pairs, LOO null has too few degrees of freedom to distinguish fabrication from routine biological similarity (most features non-differentially expressed). This test requires ≥4 condition pairs to make statistical inference. Shown here as contextual information only.
- Missing Data Pattern — Missing rate 0.0% is below 1% threshold.

Absence of a finding for a test that wasn't run does not mean absence of the pattern.

---

## DS11 — 11-rnaseq-multicondition.csv (outcome 3)

### Dataset

- Filename: 11-rnaseq-multicondition.csv
- Shape: 1500 rows × 4 columns
- Measurement type: Genomics (Counts / TPM) (user-set)
- Data type: Continuous
- Conditions: CondA, CondB, CondC (3 conditions)
- Variance-stabilising transform: log (slope=1.99, CI [1.97, 2.02] contains 1 → inconclusive → assay fallback (genomics) → log)

### Outcome calibration

Outcome 3 of 4 — Investigate closely. Significant anomalies detected. The combination of findings is unlikely to arise from chance alone. Treat as a strong screening signal warranting close investigation. Innocent explanations remain possible — the burden is on identifying a specific one that fits the patterns.

### Findings

#### High-severity findings

**Benford's Law (Second Digit)** — unusual digits cluster
What the test detects: Counts the frequency of each second digit (0–9) across all values. As with first digits, lower second digits (0, 1, 2…) appear slightly more often than higher ones, following a predictable distribution (Benford distribution). This test checks whether the data follows that pattern (chi-squared goodness-of-fit with MAD simulation).
Location: Global
Evidence: primaryP = <0.0001

**Autocorrelation** — cross-replicate cluster
What the test detects: Checks whether the inter-replicate noise is correlated between adjacent rows. In independent measurements, knowing the noise at one row tells you nothing about the next — significant correlation means the noise follows a pattern rather than varying freely (lag-1 autocorrelation, pooled t-test).
Location: Global
Evidence: primaryP = <0.0001

#### Moderate-severity findings

**Residual Spike Correlation** — copy-paste/edit cluster
What the test detects: Identifies which rows are unusually variable between replicate pairs and tests whether the same rows are noisy across multiple conditions. In typical data, noisy rows vary independently across conditions — coordinated residual spikes suggest shared structure. Significance assessed by shuffling row order (permutation test).
Location: Global
Evidence: primaryP = 0.0020

#### Cleared (within flagged clusters)

Within unusual digits cluster:
- Benford's Law (First Digit)
- Terminal Digit Uniformity
- Decimal Precision Consistency

Within cross-replicate cluster:
- Inter-Replicate Correlation
- Noise Scaling With Measurement Size
- Excess Kurtosis
- Windowed Autocorrelation
- Selective Noise Partitioning

Within copy-paste/edit cluster:
- Exact Duplicate Detection
- Constant-Offset Blocks

Cleared tests are evidence too — a test that was run and didn't fire narrows the space of explanations.

#### Other clusters — all applicable tests cleared

- Distribution shapes cluster (2 tests): Entropy / Zipf Analysis, Modality Test
- Cross-condition cluster (1 tests): Cross-Condition Consistency

#### Tests not run

- Value-Frequency Spike — Not applicable — data is primarily non-integer. This test detects anomalous frequency spikes in integer value distributions.
- Baseline Balance — Only 4 DATA columns — need ≥5 features for meaningful balance test.
- Cross-Condition Rank Correlation — With fewer than 4 condition pairs, LOO null has too few degrees of freedom to distinguish fabrication from routine biological similarity (most features non-differentially expressed). This test requires ≥4 condition pairs to make statistical inference. Shown here as contextual information only.
- Mahalanobis Row Outlier — Not applicable to genomics data. Count distributions violate the multivariate normality assumption required for χ²-based D² thresholds. Biological expression heterogeneity produces widespread outliers that are not anomalous.
- Blocked Mahalanobis — Not applicable when row order is arbitrary. This test depends on meaningful row sequence (plate position, instrument run order, dose gradient); under long-format pivots or arbitrary-order data (gene lists, alphabetised protein IDs) the per-row position carries no forensic signal.
- Column Goodness-of-Fit — All columns routed to N/A (4 columns; most common: Pre-skip: γ₁=3.06, γ₂=15.13 — family set {Normal, Poisson, NB} does not cover this shape (v1.1 extension planned)).
- Runs Test — Not applicable when row order is arbitrary. This test depends on meaningful row sequence (plate position, instrument run order, dose gradient); under long-format pivots or arbitrary-order data (gene lists, alphabetised protein IDs) the per-row position carries no forensic signal.
- Within-Row Variance — Not applicable to genomics data. Within-row variance across technical replicates of the same gene has different semantics — biological expression heterogeneity dominates.
- LOESS Residual Analysis — Not applicable when row order is arbitrary. This test depends on meaningful row sequence (plate position, instrument run order, dose gradient); under long-format pivots or arbitrary-order data (gene lists, alphabetised protein IDs) the per-row position carries no forensic signal.
- Row-Mean Runs — Not applicable when row order is arbitrary. This test depends on meaningful row sequence (plate position, instrument run order, dose gradient); under long-format pivots or arbitrary-order data (gene lists, alphabetised protein IDs) the per-row position carries no forensic signal.
- Regional Noise Homogeneity — Not applicable when row order is arbitrary. This test depends on meaningful row sequence (plate position, instrument run order, dose gradient); under long-format pivots or arbitrary-order data (gene lists, alphabetised protein IDs) the per-row position carries no forensic signal.
- Missing Data Pattern — Missing rate 0.0% is below 1% threshold.

Absence of a finding for a test that wasn't run does not mean absence of the pattern.

---

## DS12a — 12a-uniform-mixture-clean.csv (outcome 0)

§4 does not render at outcome 0; renderer returns null.

---

## DS12b — 12b-uniform-mixture-fabricated.csv (outcome 1)

### Dataset

- Filename: 12b-uniform-mixture-fabricated.csv
- Shape: 400 rows × 6 columns
- Measurement type: Unspecified / General (user-set)
- Data type: Continuous
- Conditions: Genuine, Fabricated (2 conditions)
- Variance-stabilising transform: log (Slope=2.00, 95% CI [1.92, 2.09] entirely above 1 → proportional noise → log)

### Outcome calibration

Outcome 1 of 4 — Review. Some findings warrant a closer look, but the overall pattern is within the expected false-positive range. With 24 applicable tests at α=0.01 (BH-FDR adjusted across families), occasional flags from statistical noise are anticipated. Treat individual findings as soft signal — a starting point for inspection rather than evidence of a problem. Convergence (multiple tests firing on the same column or region, or a finding aligned with a known data-handling step) would change that assessment; isolated findings often have routine explanations.

### Findings

#### High-severity findings

None.

#### Moderate-severity findings

**LOESS Residual Analysis** — cross-replicate cluster
What the test detects: Measures how variable each row's replicates are, then fits a smooth trend to see whether noise changes gradually across the dataset. A cumulative-sum test (CUSUM) pinpoints the exact row where the noise character shifts — the changepoint.
Location: 9 regions
Evidence: CUSUM changepoint: noise increases at row 196 (p=0.0020). A subset of rows has different noise character than the rest, suggesting smoothing, extrapolation, or manual editing in that region.
primaryP = 0.0020

#### Cleared (within flagged clusters)

Within cross-replicate cluster:
- Inter-Replicate Correlation
- Mahalanobis Row Outlier
- Blocked Mahalanobis
- Noise Scaling With Measurement Size
- Excess Kurtosis
- Autocorrelation
- Windowed Autocorrelation
- Runs Test
- Within-Row Variance
- Row-Mean Runs
- Selective Noise Partitioning
- Regional Noise Homogeneity

Cleared tests are evidence too — a test that was run and didn't fire narrows the space of explanations.

#### Other clusters — all applicable tests cleared

- Copy-paste/edit cluster (3 tests): Exact Duplicate Detection, Constant-Offset Blocks, Residual Spike Correlation
- Unusual digits cluster (4 tests): Benford's Law (Second Digit), Terminal Digit Uniformity, Decimal Precision Consistency, Value-Frequency Spike
- Distribution shapes cluster (2 tests): Entropy / Zipf Analysis, Modality Test
- Cross-condition cluster (2 tests): Baseline Balance, Cross-Condition Consistency

#### Tests not run

- Benford's Law (First Digit) — OOM span 1.2 < 1.5 — Benford's Law requires ≥1.5 orders of magnitude for meaningful application.
- Cross-Condition Rank Correlation — With fewer than 4 condition pairs, LOO null has too few degrees of freedom to distinguish fabrication from routine biological similarity (most features non-differentially expressed). This test requires ≥4 condition pairs to make statistical inference. Shown here as contextual information only.
- Column Goodness-of-Fit — All columns routed to N/A (6 columns; most common: Pre-skip: γ₁=3.32, γ₂=17.51 — family set {Normal, Poisson, NB} does not cover this shape (v1.1 extension planned)).
- Missing Data Pattern — Missing rate 0.0% is below 1% threshold.

Absence of a finding for a test that wasn't run does not mean absence of the pattern.

---

## DS13 — 13-vfstest-cellcountest.csv (outcome 2)

### Dataset

- Filename: 13-vfstest-cellcountest.csv
- Shape: 120 rows × 4 columns
- Measurement type: Cell Counting / Viability (user-set)
- Data type: Count / Integer
- Conditions: single condition (no condition column)
- Variance-stabilising transform: Anscombe √(x+⅜) (Integer data (100%), slope=-0.59, 95% CI [-1.69, 0.50] → Anscombe √(x+⅜))

### Outcome calibration

Outcome 2 of 4 — Investigate. The findings indicate anomalies worth investigating. Treat as a moderate screening signal — neither a single false positive nor a strong convergent pattern. Where multiple findings are present, look for whether they converge on the same column, region, or mechanism. Assess whether innocent explanations (processing steps, instrument artifacts, legitimate domain reasons) fit the specific patterns.

### Findings

#### High-severity findings

**Value-Frequency Spike** — unusual digits cluster
What the test detects: Tests whether any value in a column appears far more often than its neighbours. Hand-typed fabricated data tends to reuse "comfortable" values — round numbers, favourite digits — producing sharp frequency spikes against the smooth distribution real measurements produce. The test smooths each value’s frequency by comparing against its neighbours in value-space, then flags any value whose observed count is too high to be explained by the smoothed expectation. See METHODOLOGY.md §3.5 for the formal statement (leave-one-out neighbour smoothing; Poisson tail probability for the spike-vs-expected comparison).
Location: Global
Evidence: 2 values with anomalous frequency: 45 (25× obs, 6.5 exp, 3.8×), 34 (31× obs, 15.0 exp, 2.1×).
primaryP = <0.0001

#### Moderate-severity findings

None.

#### Cleared (within flagged clusters)

Within unusual digits cluster:
- (no other applicable tests in this cluster ran for this dataset — see "Tests not run" below)

Cleared tests are evidence too — a test that was run and didn't fire narrows the space of explanations.

#### Other clusters — all applicable tests cleared

- Copy-paste/edit cluster (2 tests): Exact Duplicate Detection, Constant-Offset Blocks
- Distribution shapes cluster (3 tests): Entropy / Zipf Analysis, Column Goodness-of-Fit, Modality Test
- Cross-replicate cluster (10 tests): Inter-Replicate Correlation, Mahalanobis Row Outlier, Excess Kurtosis, Autocorrelation, Windowed Autocorrelation, Runs Test, Within-Row Variance, LOESS Residual Analysis, Selective Noise Partitioning, Regional Noise Homogeneity

#### Tests not run

- Benford's Law (First Digit) — Not applicable to cell count data. Poisson count data from a single counting process has a mathematically determined leading-digit distribution that depends on λ, not Benford's law. Benford applicability requires data spanning multiple orders of magnitude from heterogeneous natural processes.
- Benford's Law (Second Digit) — Not applicable to cell count data. See Benford's First Digit for rationale.
- Terminal Digit Uniformity — Data is 100% integer values. Terminal digit analysis tests the last decimal digit of continuous measurements — for integer/count data, the units digit distribution depends on the count-generating process and is not expected to be uniform. Test not applicable.
- Decimal Precision Consistency — Insufficient decimal-valued data (need ≥30 non-integer values).
- Residual Spike Correlation — No condition grouping found. Residual spike correlation requires ≥2 conditions.
- Baseline Balance — Requires ≥2 experimental conditions.
- Cross-Condition Rank Correlation — Need ≥2 conditions with ≥5 rows each.
- Cross-Condition Consistency — Need ≥2 experimental conditions.
- Blocked Mahalanobis — Not applicable to count data (covariance structure null is only stable on continuous measurements).
- Noise Scaling With Measurement Size — Row means span only 0.4 orders of magnitude — need ≥1.0 for a reliable log-log slope estimate. Noise Scaling test is inapplicable for narrow-range data.
- Row-Mean Runs — Requires row-level condition labels. Without per-condition grouping, biological variation in row means produces natural clustering that cannot be distinguished from data concerns.
- Missing Data Pattern — Missing rate 0.0% is below 1% threshold.

Absence of a finding for a test that wasn't run does not mean absence of the pattern. In particular, this dataset has a single condition, so the cross-condition cluster (3 tests) and condition-dependent tests in other clusters did not run.

---

## DS14 — 14-crctest-survey.csv (outcome 3)

### Dataset

- Filename: 14-crctest-survey.csv
- Shape: 80 rows × 6 columns
- Measurement type: Survey / Likert Scale (user-set)
- Data type: Continuous
- Conditions: single condition (no condition column)
- Variance-stabilising transform: Anscombe √(x+⅜) (Integer data (100%), slope=-0.76, 95% CI [-2.25, 0.73] → Anscombe √(x+⅜))

### Outcome calibration

Outcome 3 of 4 — Investigate closely. Significant anomalies detected. The combination of findings is unlikely to arise from chance alone. Treat as a strong screening signal warranting close investigation. Innocent explanations remain possible — the burden is on identifying a specific one that fits the patterns.

### Findings

#### High-severity findings

**Exact Duplicate Detection** — copy-paste/edit cluster
What the test detects: Scans the dataset for identical values and tests the observed count against what would be expected if values were shuffled randomly (permutation test). Tests are computed independently; some duplicates may appear in more than one test.
Location: 20 blocks
Evidence: primaryP = <0.0001

**Within-Row Variance** — cross-replicate cluster
What the test detects: Computes how much replicates vary around each row's mean, then compares that spread to what the overall noise pattern predicts for a row at that signal level. Rows with unusually low spread are flagged as outliers (binomial test on z-score exceedances).
Location: 3 rows
Evidence: primaryP = <0.0001

#### Moderate-severity findings

None.

#### Cleared (within flagged clusters)

Within copy-paste/edit cluster:
- Constant-Offset Blocks

Within cross-replicate cluster:
- Inter-Replicate Correlation
- Mahalanobis Row Outlier
- Excess Kurtosis
- Autocorrelation
- Windowed Autocorrelation
- Runs Test
- LOESS Residual Analysis
- Selective Noise Partitioning
- Regional Noise Homogeneity

Cleared tests are evidence too — a test that was run and didn't fire narrows the space of explanations.

#### Other clusters — all applicable tests cleared

- Distribution shapes cluster (1 tests): Entropy / Zipf Analysis

#### Tests not run

- Benford's Law (First Digit) — OOM span 0.4 < 1.5 — Benford's Law requires ≥1.5 orders of magnitude for meaningful application.
- Benford's Law (Second Digit) — Not applicable to integer/count data. Count values naturally concentrate second significant digits at 0 (e.g. 10, 20, 100, 200) — this is an intrinsic property of integer distributions, not a meaningful signal.
- Terminal Digit Uniformity — Data is 100% integer values. Terminal digit analysis tests the last decimal digit of continuous measurements — for integer/count data, the units digit distribution depends on the count-generating process and is not expected to be uniform. Test not applicable.
- Decimal Precision Consistency — Insufficient decimal-valued data (need ≥30 non-integer values).
- Value-Frequency Spike — Only 5 distinct integer values — need ≥20 for local smoothing to distinguish genuine spikes from expected variation on small scales.
- Residual Spike Correlation — No condition grouping found. Residual spike correlation requires ≥2 conditions.
- Baseline Balance — Requires ≥2 experimental conditions.
- Cross-Condition Rank Correlation — Need ≥2 conditions with ≥5 rows each.
- Cross-Condition Consistency — Need ≥2 experimental conditions.
- Blocked Mahalanobis — Not applicable to survey data (covariance structure null is only stable on continuous measurements).
- Noise Scaling With Measurement Size — Row means span only 0.2 orders of magnitude — need ≥1.0 for a reliable log-log slope estimate. Noise Scaling test is inapplicable for narrow-range data.
- Column Goodness-of-Fit — All columns routed to N/A (6 columns; most common: < 10 distinct values).
- Modality Test — All columns routed to N/A (6 columns; most common: < 15 distinct values).
- Row-Mean Runs — Requires row-level condition labels. Without per-condition grouping, biological variation in row means produces natural clustering that cannot be distinguished from data concerns.
- Missing Data Pattern — Missing rate 0.0% is below 1% threshold.

Absence of a finding for a test that wasn't run does not mean absence of the pattern. In particular, this dataset has a single condition, so the cross-condition cluster (3 tests) and condition-dependent tests in other clusters did not run.

---

## DS15 — 15-missing-carlisle.csv (outcome 3)

### Dataset

- Filename: 15-missing-carlisle.csv
- Shape: 160 rows × 6 columns
- Measurement type: Unspecified / General (user-set)
- Data type: Continuous
- Conditions: Control, Treatment (2 conditions)
- Variance-stabilising transform: log (Slope=2.04, 95% CI [1.84, 2.24] entirely above 1 → proportional noise → log)

### Outcome calibration

Outcome 3 of 4 — Investigate closely. Significant anomalies detected. The combination of findings is unlikely to arise from chance alone. Treat as a strong screening signal warranting close investigation. Innocent explanations remain possible — the burden is on identifying a specific one that fits the patterns.

### Findings

#### High-severity findings

**Missing Data Pattern** — cross-replicate cluster
What the test detects: Checks whether missing values are scattered randomly or form patterns — for example, the same columns going missing together, one condition having more gaps than another, or a rectangular block of missing cells (Fisher’s exact and spatial clustering tests).
Location: Global
Evidence: primaryP = <0.0001

#### Moderate-severity findings

**Cross-Condition Consistency** — cross-condition cluster
What the test detects: Compares each condition pair on a set of robust distribution properties (trimmed span, dispersion, CDF shape) and flags pairs that are suspiciously close to each other — condition pairs that match more than random re-assignment of pooled values would produce. Real experimental conditions legitimately differ on location and scale, so the forensic signal for these properties lives in the "too similar" tail. Two-sided permutation null at the cell level, BH-FDR across all property × pair units; only similar-direction units contribute to the flag.
Location: Global
Evidence: primaryP = 0.0090

**Blocked Mahalanobis** — cross-replicate cluster
What the test detects: Tests whether any contiguous block of rows shows a different mean or correlation structure than the rest of the dataset. Slides a window across the data, comparing each window’s column means and inter-column correlations against the surrounding rows. A fabricated insert — rows generated with a different covariance pattern than the rest, even when individual columns look unremarkable — flags here. See METHODOLOGY.md §2.6b for the formal statement (Hotelling T² with Ledoit-Wolf covariance shrinkage; covariance-shape component via the dominant eigenvalue of the between-vs-rest covariance ratio).
Location: 2 regions
Evidence: Block covariance inflation in Control rows 1–40 (λ = 4.727, adj-p = 0.0040).
primaryP = 0.0040

#### Cleared (within flagged clusters)

Within cross-replicate cluster:
- Inter-Replicate Correlation
- Mahalanobis Row Outlier
- Noise Scaling With Measurement Size
- Excess Kurtosis
- Autocorrelation
- Windowed Autocorrelation
- Runs Test
- Within-Row Variance
- LOESS Residual Analysis
- Row-Mean Runs
- Selective Noise Partitioning
- Regional Noise Homogeneity

Within cross-condition cluster:
- Baseline Balance

Cleared tests are evidence too — a test that was run and didn't fire narrows the space of explanations.

#### Other clusters — all applicable tests cleared

- Copy-paste/edit cluster (3 tests): Exact Duplicate Detection, Constant-Offset Blocks, Residual Spike Correlation
- Unusual digits cluster (3 tests): Terminal Digit Uniformity, Decimal Precision Consistency, Value-Frequency Spike
- Distribution shapes cluster (3 tests): Entropy / Zipf Analysis, Column Goodness-of-Fit, Modality Test

#### Tests not run

- Benford's Law (First Digit) — OOM span 0.9 < 1.5 — Benford's Law requires ≥1.5 orders of magnitude for meaningful application.
- Benford's Law (Second Digit) — Data spans only 0.9 orders of magnitude — Benford's Law requires ≥1 for meaningful application.
- Cross-Condition Rank Correlation — With fewer than 4 condition pairs, LOO null has too few degrees of freedom to distinguish fabrication from routine biological similarity (most features non-differentially expressed). This test requires ≥4 condition pairs to make statistical inference. Shown here as contextual information only.

Absence of a finding for a test that wasn't run does not mean absence of the pattern.

---

## DS16 — 16-densitometry-carlisle-overbalanced.csv (outcome 2)

### Dataset

- Filename: 16-densitometry-carlisle-overbalanced.csv
- Shape: 60 rows × 18 columns
- Measurement type: Western Blot Densitometry (user-set)
- Data type: Continuous
- Conditions: Control, Treatment_A, Treatment_B (3 conditions)
- Variance-stabilising transform: log (slope=1.97, CI [1.63, 2.30] contains 1 → inconclusive → assay fallback (densitometry) → log)

### Outcome calibration

Outcome 2 of 4 — Investigate. The findings indicate anomalies worth investigating. Treat as a moderate screening signal — neither a single false positive nor a strong convergent pattern. Where multiple findings are present, look for whether they converge on the same column, region, or mechanism. Assess whether innocent explanations (processing steps, instrument artifacts, legitimate domain reasons) fit the specific patterns.

### Findings

#### High-severity findings

**Baseline Balance** — cross-condition cluster
What the test detects: When subjects are randomly assigned to groups, some measurements — like age or weight — will naturally differ a little between groups. If every measurement matches almost perfectly across groups, the assignment may not be genuinely random. This test checks whether the between-group differences are too consistently small (Carlisle's method).
Location: Global
Evidence: primaryP = <0.0001

#### Moderate-severity findings

None.

#### Cleared (within flagged clusters)

Within cross-condition cluster:
- Cross-Condition Consistency

Cleared tests are evidence too — a test that was run and didn't fire narrows the space of explanations.

#### Other clusters — all applicable tests cleared

- Copy-paste/edit cluster (3 tests): Exact Duplicate Detection, Constant-Offset Blocks, Residual Spike Correlation
- Unusual digits cluster (3 tests): Terminal Digit Uniformity, Decimal Precision Consistency, Value-Frequency Spike
- Distribution shapes cluster (3 tests): Entropy / Zipf Analysis, Column Goodness-of-Fit, Modality Test
- Cross-replicate cluster (11 tests): Inter-Replicate Correlation, Mahalanobis Row Outlier, Blocked Mahalanobis, Excess Kurtosis, Autocorrelation, Windowed Autocorrelation, Runs Test, Within-Row Variance, LOESS Residual Analysis, Selective Noise Partitioning, Regional Noise Homogeneity

#### Tests not run

- Benford's Law (First Digit) — OOM span 0.5 < 1.5 — Benford's Law requires ≥1.5 orders of magnitude for meaningful application.
- Benford's Law (Second Digit) — Data spans only 0.5 orders of magnitude — Benford's Law requires ≥1 for meaningful application.
- Cross-Condition Rank Correlation — With fewer than 4 condition pairs, LOO null has too few degrees of freedom to distinguish fabrication from routine biological similarity (most features non-differentially expressed). This test requires ≥4 condition pairs to make statistical inference. Shown here as contextual information only.
- Noise Scaling With Measurement Size — Row means span only 0.5 orders of magnitude — need ≥1.0 for a reliable log-log slope estimate. Noise Scaling test is inapplicable for narrow-range data.
- Row-Mean Runs — Need ≥2 replicate columns and ≥10 rows.
- Missing Data Pattern — Missing rate 0.0% is below 1% threshold.

Absence of a finding for a test that wasn't run does not mean absence of the pattern.

---

## DS17 — 17-densitometry-carlisle-clean.csv (outcome 0)

§4 does not render at outcome 0; renderer returns null.

---

## DS19 — 19-inheritance-fabricated.csv (outcome 3)

### Dataset

- Filename: 19-inheritance-fabricated.csv
- Shape: 1200 rows × 1 columns
- Measurement type: Unspecified / General (user-set)
- Data type: Continuous
- Conditions: Control, Treatment (2 conditions)
- Variance-stabilising transform: raw (insufficient range for slope → assay fallback (general) → raw)

### Outcome calibration

Outcome 3 of 4 — Investigate closely. Significant anomalies detected. The combination of findings is unlikely to arise from chance alone. Treat as a strong screening signal warranting close investigation. Innocent explanations remain possible — the burden is on identifying a specific one that fits the patterns.

### Findings

#### High-severity findings

None.

#### Moderate-severity findings

**Cross-Condition Consistency** — cross-condition cluster
What the test detects: Compares each condition pair on a set of robust distribution properties (trimmed span, dispersion, CDF shape) and flags pairs that are suspiciously close to each other — condition pairs that match more than random re-assignment of pooled values would produce. Real experimental conditions legitimately differ on location and scale, so the forensic signal for these properties lives in the "too similar" tail. Two-sided permutation null at the cell level, BH-FDR across all property × pair units; only similar-direction units contribute to the flag.
Location: Global
Evidence: primaryP = 0.0060

**Column Goodness-of-Fit** — distribution shapes cluster
What the test detects: For each column, fits a parametric family (Normal, Poisson, or Negative Binomial) and measures how closely the column's CDF shape matches the fit (Anderson–Darling). A parametric bootstrap with refit calibrates the expected AD²; too-large values indicate shape mismatch (hand-typed, truncated, copied from a different-shape source), too-small values indicate a too-tight fit suggesting RNG padding.
Location: Global
Evidence: primaryP = 0.0020

#### Cleared (within flagged clusters)

Within cross-condition cluster:
- (no other applicable tests in this cluster ran for this dataset — see "Tests not run" below)

Within distribution shapes cluster:
- Entropy / Zipf Analysis
- Modality Test

Cleared tests are evidence too — a test that was run and didn't fire narrows the space of explanations.

#### Other clusters — all applicable tests cleared

- Copy-paste/edit cluster (2 tests): Exact Duplicate Detection, Residual Spike Correlation
- Unusual digits cluster (2 tests): Terminal Digit Uniformity, Decimal Precision Consistency

#### Tests not run

- Benford's Law (First Digit) — OOM span 0.3 < 1.5 — Benford's Law requires ≥1.5 orders of magnitude for meaningful application.
- Benford's Law (Second Digit) — Data spans only 0.3 orders of magnitude — Benford's Law requires ≥1 for meaningful application.
- Value-Frequency Spike — Not applicable — data is primarily non-integer. This test detects anomalous frequency spikes in integer value distributions.
- Inter-Replicate Correlation — No valid within-condition replicate pairs found.
- Constant-Offset Blocks — Insufficient data (≥2 cols, ≥4 rows required).
- Baseline Balance — Only 1 DATA columns — need ≥5 features for meaningful balance test.
- Cross-Condition Rank Correlation — With fewer than 4 condition pairs, LOO null has too few degrees of freedom to distinguish fabrication from routine biological similarity (most features non-differentially expressed). This test requires ≥4 condition pairs to make statistical inference. Shown here as contextual information only.
- Mahalanobis Row Outlier — Requires ≥3 replicate columns for invertible covariance matrix.
- Blocked Mahalanobis — Not applicable when row order is arbitrary. This test depends on meaningful row sequence (plate position, instrument run order, dose gradient); under long-format pivots or arbitrary-order data (gene lists, alphabetised protein IDs) the per-row position carries no forensic signal.
- Noise Scaling With Measurement Size — Need ≥3 replicate columns to estimate within-row variance.
- Excess Kurtosis — Insufficient data (≥2 cols, ≥20 rows).
- Autocorrelation — Insufficient data (≥2 cols, ≥10 rows).
- Windowed Autocorrelation — Need ≥2 replicate columns for paired differences.
- Runs Test — Not applicable when row order is arbitrary. This test depends on meaningful row sequence (plate position, instrument run order, dose gradient); under long-format pivots or arbitrary-order data (gene lists, alphabetised protein IDs) the per-row position carries no forensic signal.
- Within-Row Variance — Need ≥3 replicate columns for meaningful within-row SD.
- LOESS Residual Analysis — Not applicable when row order is arbitrary. This test depends on meaningful row sequence (plate position, instrument run order, dose gradient); under long-format pivots or arbitrary-order data (gene lists, alphabetised protein IDs) the per-row position carries no forensic signal.
- Row-Mean Runs — Not applicable when row order is arbitrary. This test depends on meaningful row sequence (plate position, instrument run order, dose gradient); under long-format pivots or arbitrary-order data (gene lists, alphabetised protein IDs) the per-row position carries no forensic signal.
- Selective Noise Partitioning — No conditions with sufficient data.
- Regional Noise Homogeneity — Not applicable when row order is arbitrary. This test depends on meaningful row sequence (plate position, instrument run order, dose gradient); under long-format pivots or arbitrary-order data (gene lists, alphabetised protein IDs) the per-row position carries no forensic signal.
- Missing Data Pattern — Insufficient data for missingness analysis.

Absence of a finding for a test that wasn't run does not mean absence of the pattern.

---

## DS20 — 20-bimodal-fab.csv (outcome 3)

### Dataset

- Filename: 20-bimodal-fab.csv
- Shape: 300 rows × 8 columns
- Measurement type: Unspecified / General (user-set)
- Data type: Continuous
- Conditions: Control, Treatment (2 conditions)
- Variance-stabilising transform: raw (slope=1.91, CI [1.59, 2.23] contains 1 → inconclusive → assay fallback (general) → raw)

### Outcome calibration

Outcome 3 of 4 — Investigate closely. Significant anomalies detected. The combination of findings is unlikely to arise from chance alone. Treat as a strong screening signal warranting close investigation. Innocent explanations remain possible — the burden is on identifying a specific one that fits the patterns.

### Findings

#### High-severity findings

**Selective Noise Partitioning** — cross-replicate cluster
What the test detects: Checks whether all replicate columns have similar variability. A significant result means the noise levels are unequal (Bartlett's test), but with few columns it may not be possible to pinpoint which one differs.
Location: Global
Evidence: primaryP = <0.0001

#### Moderate-severity findings

**Column Goodness-of-Fit** — distribution shapes cluster
What the test detects: For each column, fits a parametric family (Normal, Poisson, or Negative Binomial) and measures how closely the column's CDF shape matches the fit (Anderson–Darling). A parametric bootstrap with refit calibrates the expected AD²; too-large values indicate shape mismatch (hand-typed, truncated, copied from a different-shape source), too-small values indicate a too-tight fit suggesting RNG padding.
Location: Global
Evidence: primaryP = 0.0035

**Autocorrelation** — cross-replicate cluster
What the test detects: Checks whether the inter-replicate noise is correlated between adjacent rows. In independent measurements, knowing the noise at one row tells you nothing about the next — significant correlation means the noise follows a pattern rather than varying freely (lag-1 autocorrelation, pooled t-test).
Location: Global
Evidence: primaryP = 0.0017

#### Cleared (within flagged clusters)

Within cross-replicate cluster:
- Inter-Replicate Correlation
- Mahalanobis Row Outlier
- Blocked Mahalanobis
- Noise Scaling With Measurement Size
- Excess Kurtosis
- Windowed Autocorrelation
- Runs Test
- Within-Row Variance
- LOESS Residual Analysis
- Row-Mean Runs
- Regional Noise Homogeneity

Within distribution shapes cluster:
- Entropy / Zipf Analysis
- Modality Test

Cleared tests are evidence too — a test that was run and didn't fire narrows the space of explanations.

#### Other clusters — all applicable tests cleared

- Copy-paste/edit cluster (3 tests): Exact Duplicate Detection, Constant-Offset Blocks, Residual Spike Correlation
- Unusual digits cluster (3 tests): Benford's Law (Second Digit), Terminal Digit Uniformity, Decimal Precision Consistency
- Cross-condition cluster (2 tests): Baseline Balance, Cross-Condition Consistency

#### Tests not run

- Benford's Law (First Digit) — Not positive-scale data (38% positive) — Benford's Law assumes a positive-scale multiplicative process.
- Value-Frequency Spike — Not applicable — data is primarily non-integer. This test detects anomalous frequency spikes in integer value distributions.
- Cross-Condition Rank Correlation — With fewer than 4 condition pairs, LOO null has too few degrees of freedom to distinguish fabrication from routine biological similarity (most features non-differentially expressed). This test requires ≥4 condition pairs to make statistical inference. Shown here as contextual information only.
- Missing Data Pattern — Missing rate 0.0% is below 1% threshold.

Absence of a finding for a test that wasn't run does not mean absence of the pattern.

---

## DS21 — 21-localised-ar.csv (outcome 3)

### Dataset

- Filename: 21-localised-ar.csv
- Shape: 400 rows × 8 columns
- Measurement type: Unspecified / General (user-set)
- Data type: Continuous
- Conditions: Control, Treatment (2 conditions)
- Variance-stabilising transform: raw (slope=1.35, CI [1.11, 1.58] contains 1 → inconclusive → assay fallback (general) → raw)

### Outcome calibration

Outcome 3 of 4 — Investigate closely. Significant anomalies detected. The combination of findings is unlikely to arise from chance alone. Treat as a strong screening signal warranting close investigation. Innocent explanations remain possible — the burden is on identifying a specific one that fits the patterns.

### Findings

#### High-severity findings

**Blocked Mahalanobis** — cross-replicate cluster
What the test detects: Tests whether any contiguous block of rows shows a different mean or correlation structure than the rest of the dataset. Slides a window across the data, comparing each window’s column means and inter-column correlations against the surrounding rows. A fabricated insert — rows generated with a different covariance pattern than the rest, even when individual columns look unremarkable — flags here. See METHODOLOGY.md §2.6b for the formal statement (Hotelling T² with Ledoit-Wolf covariance shrinkage; covariance-shape component via the dominant eigenvalue of the between-vs-rest covariance ratio).
Location: 6 regions
Evidence: Block mean shift in Control rows 81–110 (T² = 130.093, adj-p = 0.0008).
primaryP = 0.0008

**Autocorrelation** — cross-replicate cluster
What the test detects: Checks whether the inter-replicate noise is correlated between adjacent rows. In independent measurements, knowing the noise at one row tells you nothing about the next — significant correlation means the noise follows a pattern rather than varying freely (lag-1 autocorrelation, pooled t-test).
Location: Global
Evidence: primaryP = <0.0001

**Runs Test** — cross-replicate cluster
What the test detects: For each replicate pair, tracks which replicate has the larger value at each row and checks how often the lead switches. Too few switches means one replicate stays consistently above the other — either across the full column or within localised stretches (Wald–Wolfowitz runs test).
Location: Global
Evidence: primaryP = 0.0004

**Row-Mean Runs** — cross-replicate cluster
What the test detects: Within each condition, computes the average value for each row and tracks how often it crosses the condition-wide mean. Too few crossings indicates trending or block shifts; too many suggests unusual alternation (Wald–Wolfowitz runs test).
Location: Global
Evidence: primaryP = 0.0007

#### Moderate-severity findings

**Regional Noise Homogeneity** — cross-replicate cluster
What the test detects: Divides the replicate columns into spatial blocks and checks whether some blocks are more or less variable than others. Significance assessed by shuffling row order (Levene’s test with permutation null).
Location: 2 regions
Evidence: Column-vs-global windowed scan detects locally reduced noise in column 6 (scan p=0.0020). Most anomalous window: rows 81–95 (19.1× deviation from column's global variance).
primaryP = 0.0020

#### Cleared (within flagged clusters)

Within cross-replicate cluster:
- Inter-Replicate Correlation
- Mahalanobis Row Outlier
- Noise Scaling With Measurement Size
- Excess Kurtosis
- Windowed Autocorrelation
- Within-Row Variance
- LOESS Residual Analysis
- Selective Noise Partitioning

Cleared tests are evidence too — a test that was run and didn't fire narrows the space of explanations.

#### Other clusters — all applicable tests cleared

- Copy-paste/edit cluster (3 tests): Exact Duplicate Detection, Constant-Offset Blocks, Residual Spike Correlation
- Unusual digits cluster (3 tests): Benford's Law (Second Digit), Terminal Digit Uniformity, Decimal Precision Consistency
- Distribution shapes cluster (3 tests): Entropy / Zipf Analysis, Column Goodness-of-Fit, Modality Test
- Cross-condition cluster (2 tests): Baseline Balance, Cross-Condition Consistency

#### Tests not run

- Benford's Law (First Digit) — Not positive-scale data (50% positive) — Benford's Law assumes a positive-scale multiplicative process.
- Value-Frequency Spike — Not applicable — data is primarily non-integer. This test detects anomalous frequency spikes in integer value distributions.
- Cross-Condition Rank Correlation — With fewer than 4 condition pairs, LOO null has too few degrees of freedom to distinguish fabrication from routine biological similarity (most features non-differentially expressed). This test requires ≥4 condition pairs to make statistical inference. Shown here as contextual information only.
- Missing Data Pattern — Missing rate 0.0% is below 1% threshold.

Absence of a finding for a test that wasn't run does not mean absence of the pattern.

---

## DS22 — 22-covariance-block.csv (outcome 2)

### Dataset

- Filename: 22-covariance-block.csv
- Shape: 400 rows × 7 columns
- Measurement type: Unspecified / General (user-set)
- Data type: Continuous
- Conditions: Control, Treatment (2 conditions)
- Variance-stabilising transform: raw (slope=1.43, CI [1.18, 1.68] contains 1 → inconclusive → assay fallback (general) → raw)

### Outcome calibration

Outcome 2 of 4 — Investigate. The findings indicate anomalies worth investigating. Treat as a moderate screening signal — neither a single false positive nor a strong convergent pattern. Where multiple findings are present, look for whether they converge on the same column, region, or mechanism. Assess whether innocent explanations (processing steps, instrument artifacts, legitimate domain reasons) fit the specific patterns.

### Findings

#### High-severity findings

**Runs Test** — cross-replicate cluster
What the test detects: For each replicate pair, tracks which replicate has the larger value at each row and checks how often the lead switches. Too few switches means one replicate stays consistently above the other — either across the full column or within localised stretches (Wald–Wolfowitz runs test).
Location: Global
Evidence: primaryP = 0.0002

#### Moderate-severity findings

**Blocked Mahalanobis** — cross-replicate cluster
What the test detects: Tests whether any contiguous block of rows shows a different mean or correlation structure than the rest of the dataset. Slides a window across the data, comparing each window’s column means and inter-column correlations against the surrounding rows. A fabricated insert — rows generated with a different covariance pattern than the rest, even when individual columns look unremarkable — flags here. See METHODOLOGY.md §2.6b for the formal statement (Hotelling T² with Ledoit-Wolf covariance shrinkage; covariance-shape component via the dominant eigenvalue of the between-vs-rest covariance ratio).
Location: 5 regions
Evidence: Block covariance inflation in Control rows 81–110 (λ = 2.494, adj-p = 0.0056).
primaryP = 0.0056

**Autocorrelation** — cross-replicate cluster
What the test detects: Checks whether the inter-replicate noise is correlated between adjacent rows. In independent measurements, knowing the noise at one row tells you nothing about the next — significant correlation means the noise follows a pattern rather than varying freely (lag-1 autocorrelation, pooled t-test).
Location: Global
Evidence: primaryP = 0.0012

#### Cleared (within flagged clusters)

Within cross-replicate cluster:
- Inter-Replicate Correlation
- Mahalanobis Row Outlier
- Noise Scaling With Measurement Size
- Excess Kurtosis
- Windowed Autocorrelation
- Within-Row Variance
- LOESS Residual Analysis
- Row-Mean Runs
- Selective Noise Partitioning
- Regional Noise Homogeneity

Cleared tests are evidence too — a test that was run and didn't fire narrows the space of explanations.

#### Other clusters — all applicable tests cleared

- Copy-paste/edit cluster (3 tests): Exact Duplicate Detection, Constant-Offset Blocks, Residual Spike Correlation
- Unusual digits cluster (3 tests): Benford's Law (Second Digit), Terminal Digit Uniformity, Decimal Precision Consistency
- Distribution shapes cluster (3 tests): Entropy / Zipf Analysis, Column Goodness-of-Fit, Modality Test
- Cross-condition cluster (2 tests): Baseline Balance, Cross-Condition Consistency

#### Tests not run

- Benford's Law (First Digit) — Not positive-scale data (51% positive) — Benford's Law assumes a positive-scale multiplicative process.
- Value-Frequency Spike — Not applicable — data is primarily non-integer. This test detects anomalous frequency spikes in integer value distributions.
- Cross-Condition Rank Correlation — With fewer than 4 condition pairs, LOO null has too few degrees of freedom to distinguish fabrication from routine biological similarity (most features non-differentially expressed). This test requires ≥4 condition pairs to make statistical inference. Shown here as contextual information only.
- Missing Data Pattern — Missing rate 0.0% is below 1% threshold.

Absence of a finding for a test that wasn't run does not mean absence of the pattern.
